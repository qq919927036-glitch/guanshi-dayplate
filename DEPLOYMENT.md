# 公开上线与 Google 收录

公开 HTTPS 地址为 `https://qq919927036-glitch.github.io/guanshi-dayplate/`。

## 已完成的收录准备

- HTML 已允许抓取与索引，并包含中文 SEO 元数据、Open Graph 和 Schema.org `WebApplication` 结构化数据。
- 根目录的 `robots.txt` 允许所有爬虫抓取。
- `sitemap.xml` 已列出最终公开首页，并在 `robots.txt` 中声明。

## 上线后必须完成

1. 在 Google Search Console 添加并验证 `https://qq919927036-glitch.github.io/guanshi-dayplate/`。
2. 在 Search Console 的 Sitemaps 报告提交 `https://qq919927036-glitch.github.io/guanshi-dayplate/sitemap.xml`。
3. 确保主页、`robots.txt` 和 `sitemap.xml` 都返回 HTTP 200。
4. 用 URL Inspection 检查首页，通过实时测试后点击“Request indexing”。

Google 是否将页面加入索引由其爬虫决定；提交站点地图和请求抓取可以发现页面，但不能保证即时或必然收录。
